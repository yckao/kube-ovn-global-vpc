apiVersion: networking.globalvpc.io/v1alpha1
kind: SiteVpc
metadata:
  # SiteVpc is cluster-scoped. Apply this file only to Site B's API.
  name: tenant-red-local
spec:
  globalVpcID: tenant-red
  siteID: site-b
  attachmentID: infra-b
  cidr: 10.242.0.0/24
  gateway: 10.242.0.1
